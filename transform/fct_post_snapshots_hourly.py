# (later: figure out which "snapshotted_on" and "snapshot_hour" combinations we need)
# For a given snapshotted_on and snapshot_hour (for now, hardcode it)
# Create fact table if it doesn't exist
# Create partition if it doesn't exist
# Insert SQL